<UserControl x:Class="SigiFluent.Views.UserControls.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
         <StackPanel>
             <TextBlock Style="{DynamicResource Error_msg}" Text="{Binding Message}" Margin="10" TextWrapping="Wrap" />
             <Button Content="Guardar" Margin="10" Style="{DynamicResource GrayButton}" HorizontalContentAlignment="Center"/>
            <Button Content="Cancelar" Margin="10"   Style="{DynamicResource GrayButton}" HorizontalContentAlignment="Center"/>
        </StackPanel>   
    </Grid>
</UserControl>
